{
  "name": "arc-board",
  "private": true,
  "version": "0.0.0-development",
  "description": "arc-board — MCP-driven story queue board. Repo-tooling root (release automation + commit linting). The application lives in arc-story-queue/.",
  "type": "module",
  "scripts": {
    "release": "semantic-release",
    "commitlint": "commitlint"
  },
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "^21.2.0",
    "@commitlint/config-conventional": "^21.2.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "semantic-release": "^25.0.5"
  }
}
